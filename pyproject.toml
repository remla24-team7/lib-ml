[tool.poetry]
name = "lib-ml"
version = "0.1.0"
description = "lib-ml"
authors = []
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
keras = "^3.3.3"
tensorflow = "^2.16.1"
scikit-learn = "^1.4.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
